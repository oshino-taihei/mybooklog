- provide :title, "#{@user.name}の本棚"
.user
  %h1 #{@user.name}の本棚
  .books-num
    %p 登録アイテム数: #{@user.reviews.size}
  .follows
    .follow-to= follow_to(@user)
    #{@user.following.size}フォロー | #{@user.followers.size}フォロワー

.book-shelf.clearfix
  - @user.reviews.each do |review|
    .book-item
      = link_to image_tag(review.book.medium_image), edit_book_review_path(review.book)
      .book-rank
        = rank_star(review)
